<a class="card text-decoration-none text-dark mb-4" href="/groups/{{id}}">
  <div class="card-body">
    <h5 class="card-title">{{Group.name}}</h5>
    <h6 class="card-subtitle mb-2 text-muted">{{length Group.Members}} Members</h6>
    <p class="card-text">{{Group.description}}</p>
  </div>
</a>